{% extends 'partials/base.html.twig' %}
{% block content %}
<section id="about">
    <div class="row">
        <div class="nine columns main-col">
            <h2>{{ page.title }}</h2>
            {{ page.content }}
        </div>
    </div>
</section>
{% endblock %}
